<%= form_with(model: site_feature, local: true) do |form| %>
  <% if site_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_feature.errors.count, "error") %> prohibited this site_feature from being saved:</h2>

      <ul>
      <% site_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
